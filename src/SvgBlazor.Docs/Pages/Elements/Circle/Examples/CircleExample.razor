@namespace SvgBlazor.Docs.Examples

<SvgCircle CenterX="100" CenterY="100" Radius="radius" OnClick="OnClicked" />

@code {
    int radius = 20;

    void OnClicked(MouseEventArgs args)
    {
        radius+=10;
        if (radius>90)
        {
            radius = 20;
        }
    }
}