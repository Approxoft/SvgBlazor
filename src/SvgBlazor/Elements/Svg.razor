@namespace SvgBlazor
@inherits SvgElementBase

<svg id="@Id"
     class="@Class"
     style="@Style"
     viewBox="@(string.Format("0 0 {0} {1}", ViewBoxWidth ?? Width, ViewBoxHeight ?? Height))"
     width="@Width"
     height="@Height">
    @ChildContent
</svg>
